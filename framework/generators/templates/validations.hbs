import Joi from "joi";
const {DIALECT} = process.env;

export default {
	create{{ moduleName }}: function () {
		return {
		
		}
	},
	update{{ moduleName }}: function () {
		return {
		}
	},
	delete{{ moduleName }}: function () {
		return {
		}
	},
	{{ moduleName }}: function () {
		return {
		}
	},
}